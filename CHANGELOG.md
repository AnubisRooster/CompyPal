# Changelog

## 2026-09-06

- ci: add GitNexus/Graphify/Changelog hooks and workflows (e4529e1)
- Add realistic ElevenLabs voice, fix lip-sync and the backward elbow (46a84c1)
- Fix puffer-fish face and weak bow/jump/hand-to-chest gestures (af54c7e)
- Emotion-aware idle, live typing reactions, and an animation-clip pipeline (c5c2c35)
- Make the avatar's face and behavior feel alive (1838031)
- Add command-driven movements (twirl, spin, jump, bow, dance, etc.) (661fa6e)
- Add arm gestures and a greeting wave to the avatar (3028e51)
- Bring GLB avatar to life: idle pose, gaze, and gestures (34e9b2d)
- Show full-body Riven avatar instead of cropped portrait (6f8e8f4)
- Fix invisible GLB avatar: frame and scale loaded model to the camera (4027211)
- Fix empty model catalog blocking chat + add model UI in Settings (e1a888e)
- Fix migration ordering, chat request duplication, launch seeding race, and other bugs (bb134eb)
- Implement default companion loading in ContentView (5d6ae00)
- Restore CompanionApp.swift, revert project.yml to keep all source files under Companion/ (cee8e1c)
- Fix structure issues: move CompanionApp to App/, fix project.yml, resolve duplicate file errors (c9135fa)
- Add import UIKit to AvatarViewModel.swift (defensive fix for UIAccessibility dependency) (3ef0d65)
- Add comprehensive integration tests for MemoryStore, fix DatabaseManager for in-memory testing, enhance TTSEngine buffer format guard tests (c1c65f3)
- v10 review fixes: seeding race, isValidKey, SSE retry UX, Duration backoff, reduce motion observer, prewarm on foreground (71b63a5)
- Phase 4: Accessibility pass — VoiceOver, Dynamic Type, Reduce Motion (7ad4ff8)
- Phase 4: Keychain edge cases — 401 detection, key validation, throwing read (45426da)
- Phase 4: SSE reconnection — auto-retry with exponential backoff on dropped streams (54acfb6)
- Phase 4: First-token latency < 1.5s — parallel DB, prewarm, WAL, URLSession tuning (ecfbdff)
- v8 review fixes: integration test, seeding at launch, GLB snapshot, dedup, ordering (539a6e8)
- Add app icon — purple-blue gradient with glowing minimalist face (ca5d49c)
- v7 review fixes: multi-morpher index, GLB root loading, vowel fallback, cancellation token, integration test (ed91a87)
- Fix seed companions not appearing: CURRENT_TIMESTAMP date format was incompatible with GRDB's ISO-8601 parser; add migration v3 to fix existing rows, remove CURRENT_TIMESTAMP defaults, add explicit Date() to all INSERTs, include glb_asset in companions() SELECT (5320db7)
- Replace ASCII art screenshots with real simulator screenshots (Chat, Companions, Settings) (a63b037)
- Add comprehensive README with simulated screenshots, architecture docs, setup guide, and feature matrix (f6e679d)
- v6 review fixes: rewrite recoverMorphTargetNames to check meshes[*].extras.targetNames (Riven's location) with proper mesh→morpher pairing; split energiesCallback from rangeCallback; async PCM synthesis via Task.detached; time-based lip-sync sampling via display-link; reference-image fallback checks face/head/skin; buffer format guard; 26 new integration tests (a49a3e0)
- v5 review fixes: restore PCM lip sync with AVSpeechSynthesizer.write buffer analysis, add setViseme to enqueueEnergy, GLB morph target name fallback from JSON extras, v2 glb_asset migration, fix applyReferenceImage face gate, didCancel calls completion, tolerant parser strips fences, move VoicePicker to own file, avatar tests (b957a73)
- v4 review fixes: GLTFKit2 loader, willSpeakRangeOfSpeechString beats, gender-aware voice, tolerant parser, glb_asset column, morpher index, +more (06c1377)
- Code review fixes: MDLAsset GLB loader, recursive morpher, PCM lip sync, structured performance tracks, trait synonyms, runtime voice selection, +more (a3f6f88)
- Add Riven GLB model with VRM morph target rig mapping (57 blend shapes), auto-load for Riven companion (4795e11)
- Seed Atlas (male) + Riven (female) companions on first launch with appearance-based avatar thumbnails (0964717)
- Phase A0-A6: Avatar subsystem with SceneKit controller, behavior systems, performance director, wardrobe, and chat integration (1c5d7ad)
- Re-review v2 fixes: base64 ref images, image models catalog, bootstrap gen, regex intent gate, decode tests, GRDB in Package.swift (0781cc4)
- Code review fixes: OpenRouter schema alignment, catalog refresh, on-device STT, image gen identity, intent gating, +13 more (4fd3232)
- Phase 4: Production readiness — offline state, streaming cancellation, STT plugin arch, companions create view (0495699)
- Phase 3.5: Identity-reference image generation (feature-flagged) (e0e1ec3)
- Phase 3: Appearance mutation via LLM intent parsing (c985b77)
- Phase 2: Voice & Avatar — TTS, STT, 3D parametric avatar, lip sync (58b1fb8)
- Phase 1: Text companion with memory graph, persona assembly, chat streaming (a5eb884)
- Phase 0: BYOK, Catalog, Selection policy, on-device scaffold (e2de50c)
- Production readiness: Dockerfile hardening, CI/CD, env validation, iOS signing (2d19a2b)
- Fix Neo4j auth: enable authentication, upgrade to neo4j 6.x driver (75eb02e)
- Phase 4: polish, reconnection, rate limiting, logging, caching (08b02ff)
- Phase 2 + Phase 3: voice, avatar, appearance mutation (6754543)
- Phase 0 + Phase 1: scaffold, backend with Neo4j graph, iOS app, API endpoints, memory layer (592e4cd)

